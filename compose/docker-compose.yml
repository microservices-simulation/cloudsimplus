version: '3.8'

services:
  mysql:
    image: mysql:8
    container_name: mysql-grafana
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data_grafana:/var/lib/mysql

volumes:
  mysql_data_grafana: